#include <stdio.h>
#include <string.h>

/**
 * main - Use of printf thath output to the STDOUT
 *
 * Return: Always 0
 */
int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
